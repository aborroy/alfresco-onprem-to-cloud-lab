# Path to your local clone of Alfresco Dockerfiles Bakery
# Example: /Users/you/work/alfresco-dockerfiles-bakery
BAKERY_DIR=

# Image publishing coordinates
# Use REGISTRY=localhost for local-only builds
REGISTRY=localhost
REGISTRY_NAMESPACE=alfresco
TAG=25.3.0-custom

# Bakery build inputs
ACS_VERSION=25
TARGETARCH=linux/amd64

# Optional build flags (set to 1 to enable)
# BAKE_NO_CACHE=1
# BAKE_NO_PROVENANCE=1

# Default targets for scripts/build-enterprise-images.sh
TARGETS="repo share search_enterprise ats tengines sync adf_apps"
